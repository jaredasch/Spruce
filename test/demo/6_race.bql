let shared x : int = 0;


func sneakyOtherThread() -> string {
     for (i : int in range(20000)) {
        x = x + 1;
     }
     return "this can be <= 600";
}


let s1: string = fork(sneakyOtherThread);
let s2: string = fork(sneakyOtherThread);


wait(s1);
wait(s2);

return x;
