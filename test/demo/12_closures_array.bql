func makeCounter () -> function {
    let i : int = 0;
    let count : function = func () -> int {
        i = i + 1;
        return i;
    };
    return count;
}

let x : [function] = [makeCounter(), makeCounter()];